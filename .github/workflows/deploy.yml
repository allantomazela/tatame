name: Deploy Automatico Tatame

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 1. Baixando código do GitHub
      uses: actions/checkout@v2

    - name: 2. Conectando e Atualizando Servidor
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          echo "🚀 Iniciando atualização..."
          
          # Entra na pasta do projeto
          cd /var/www/tatame
          
          # Baixa as novidades e garante que está igual ao GitHub
          git fetch --all
          git reset --hard origin/main
          
          # Instala dependências
          npm install
          
          # Cria o site (Build)
          npm run build
          
          # Garante permissões
          chown -R www-data:www-data /var/www/tatame
          
          echo "✅ Site atualizado com sucesso!"